int main()
{
  decl
   int pid1,pid2,temp,i;
  enddecl
  begin
  pid =fork();

  if(pid1==0||pid2==0)then
  i=0;
  while(i<50)do
  temp=exposcall("Wrie",-2,i);
  i=i+1;
  endwhile;
  else
  i=50;
  while(i<100)do
  temp=exposcall("Wrie",-2,i);
  i=i+1;
  endwhile;
  endif;
  return 0;
  end
}

